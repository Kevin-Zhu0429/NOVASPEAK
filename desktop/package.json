{
  "name": "novaspeak-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "NovaSpeak Windows 桌面客户端",
  "author": "NovaSpeak",
  "main": "main.js",
  "scripts": {
    "dev": "electron .",
    "start": "electron .",
    "test": "node --test",
    "dist:win": "electron-builder --win nsis portable --x64"
  },
  "devDependencies": {
    "electron": "^43.1.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "top.novagaming.novaspeak",
    "productName": "NovaSpeak",
    "artifactName": "NovaSpeak-${version}-${arch}-Setup.${ext}",
    "asar": true,
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "main-window-policy.js",
      "preload.js",
      "netease-login.js",
      "netease-login-policy.js",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        },
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": "always",
      "createStartMenuShortcut": true
    },
    "portable": {
      "artifactName": "NovaSpeak-${version}-${arch}-Portable.${ext}"
    }
  }
}
